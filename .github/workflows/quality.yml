name: Quality Checks

on:
  push:
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_DATABASE: support_db
          MYSQL_USER: support_user
          MYSQL_PASSWORD: support_password
          MYSQL_ROOT_PASSWORD: root_password
        ports:
          - 3307:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -u root -proot_password"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    env:
      DATABASE_URL: mysql+pymysql://support_user:support_password@127.0.0.1:3306/support_db

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install flake8 pytest
<<<<<<< HEAD
=======

      - name: Check installed tools
        run: |
          python -m flake8 --version
          python -m pytest --version
>>>>>>> a4ff5df48b39988b1849002ff12021b7d09359fb

      - name: Run Flake8
        run: python -m flake8 . --exclude=.venv

      - name: Run PyTest
        run: python -m pytest -v
